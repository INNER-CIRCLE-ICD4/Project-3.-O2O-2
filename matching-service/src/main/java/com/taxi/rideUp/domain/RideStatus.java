package com.taxi.rideUp.domain;

/**
 * packageName : com.taxi.rideUp.domain
 * fileName    : RideStatus
 * author      : ckr
 * date        : 25. 8. 4.
 * description :
 */
public enum RideStatus {
    PENDING, MATCHED, STARTED, ENDED, CANCELLED
}
